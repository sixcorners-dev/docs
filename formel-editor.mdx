---
title: "Der Formeln-Editor im Lesson Organizer"
description: "Kurzanleitung mit Formelbeispielen für deinen Unterricht."
sidebarTitle: "Formel Editor"
icon: "square-root-variable"
---

## **Worum geht's?**

Der Formel-Editor ermöglicht dir, mathematische Ausdrücke direkt im Textfeld sauber zu setzen.

Er ermöglicht dir:

- **optische Hervorhebung** im Text (z.B. Materialhinweise)
- **mathematische Schreibweise** von gängigen Zeichen
- **Sonderzeichen** für z.B. Musik, Chemie, Physik, Mathematik (α, β, → , \<, = …)
- optisch schöne Darstellung von Matrizen, Wurzeln, Integralen, ...

### **Wie starte ich die Formeleingabe?**

Du kannst Formeln auf drei Arten eingeben:

#### 1) Quick-Version

✔️ Die einfachste Methode

→ Ideal, zur optischen Hervorhebung.

▶️ So geht’s (Schritt für Schritt)

1. Schreibe deinen Text ganz normal.
2. Markiere das Wort oder die Stelle, die eine Formel sein soll.
3. Öffne das Dreipunkt-Menü.
4. Klicke auf „Formel“ (√).
5. Fertig – der Text wird automatisch als Formel dargestellt.

📝 Beispiel

- Du schreibst: „Die Fläche ist `a²`.“
- Markierst `a²`
- Klickst auf „Formel“ → und erhältst `a²`

#### 2) Simple-Version

✔️ Die Methode, um übersichtlich Formeln oder Sonderzeichen zu schreiben.

→ Ideal, wenn du direkt eine Formel schreiben möchtest.

Hier hilft dir der Editor aktiv – du musst nur auswählen.

▶️ So geht’s

1. Mache eine neue Zeile.
2. Tippe /.
3. Wähle „Formel“.
4. Lösche den `\\LateX` Schriftzug im grauen Kasten -\> da entsteht deine Formel.
5. Schreibe simple Formeln (`a^2 \= 4`) direkt rein.
6. Beginne mit einem Backslash \\, gefolgt von einem Befehl, um Sonderzeichen oder Formeln abzurufen. (`\\alpha` → Übersetzt den Befehl "alpha" in das Symbol), (\\sum -\> erstellt ein Summenzeichen)
7. schreibe hinter einen Befehl in geschweifte Klammern, worauf sich dieser bezieht. (`\\suma+b` → in der Summe soll `a+b` stehen)

📝 Beispiele

- `\\alpha → α`
- `\\sqrtx → √(x)`
- `\\sum → ∑`
- `\\suma^2 → ∑a^2`
- `\\rightarrowabc`

#### 3) Pro-Version

✔️ Für alle, die Formeln direkt in den Text schreiben wollen. Wie Version 2, nur, schaltest du den Formeleditor selbst ein - mit der Tastatur - indem den Text mit `$...$` einrahmst.

→ Ideal, wenn du öfter Formeln brauchst oder schneller arbeiten möchtest.

▶️ So geht’s

- Inline-Formel (im Text): `$...$`
- Formelblock (eigene Zeile): `$$...$$`

📝 Beispiel

Wenn du schreibst:

`$ ...$`

dann erscheint:

`a² \+ b² \= c²`

Eine vollstädige Liste mit Befehlen und Symbolen findest Du [hier](https://katex.org/docs/supported).